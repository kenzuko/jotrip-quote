{
  "name": "jotrip-quote",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.mjs",
    "hash:admin": "node tools/make-admin-hash.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.40.0"
  }
}
